"""
Future Builder1 (side-by-side v1) implementation placeholder.

NOT imported by app.py, routes, workers, or the video engine — preparation only.
Does not import `engine.side_by_side_v1` (avoids loading the large legacy module).

Next steps (when rebuilding): implement phases here or split into submodules, then
switch `app.py` imports in a controlled change after dependency isolation for video.
"""

# Intentionally empty: phase TODOs live in project docs / tickets until implementation.
